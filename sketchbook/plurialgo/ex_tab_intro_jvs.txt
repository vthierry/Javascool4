void main() {
	// les donnees
	double [] tab = {12.5, 10.5, 10}; 
	int n; 
	n = 3;
	// les autres variables
	double total; 
	int k;
	// calcul et affichage du total du tableau
	total = 0;
	for(k=0; k<n; k=k+1) { 
		total = total + tab[k];
	}
	println("Le prix total vaut " + total); 
} 
