#summary Les points en cours de développement pour JavaScool v4.

 * *[http://code.google.com/p/javascool/issues/list Issues en cours]*
<wiki:toc max_depth="2" />

*Notre planning*
  * Je 1 Sept au soir : JVS4 RC1 partagée avec les co-développeurs
  * Ve 9 Sept au soir : JVS4 RC2 publiée en version principale sur le site 
  * Après vacances de la Toussaint : JVs4 RC3 avec améliorations demandées par les utilisateurs

----

= Développements ultérieurs et/ou en réflexion =

Status: 
  * (`*`1) à faire dès que possible, 
  * (`*`2) à spécifier avant de mettre en action, 
  * (`*`3) à rediscuter avant de fixer, 
  * (`*`4) juste une idée à pas oublier.

== Javas'Cool Core ==

  * (`*`2) Ajouter la possibilité de lancer les programmes jvs en jar, a travers l'interface save-as *.jar (et aussi *.java)

  * (`*`1) Ajouter un CRTL+F search/replace à l'éditeur

  * (`*`1) Rendre possible la cŕeation de plusieurs instances de consoles qui visualisent toutes System.out.println et Macros.stdout.prinln
    * appli à commSerie et ProgletsBuilderPanel

  * (`*`1) Création d'un lanceur .exe et .app pour le lancement de jascool [http://www.excelsior-usa.com/articles/java-to-exe.html ref] (sauf si le jnlp peut gérer les pbs de chargement dynamique)
    * http://informagen.com/JarBundler

  * (`*`3) Implementer le fait qu'on ajoute @proglet:nom-de-la-proglet.
    
  * (`*`3) Intégrer un installateur des .dll/.so/.jnilib dans le buidler pour les proglets à extensions.
    
  * (`*`4) Virer le `HtmlDisplay` HTML3 et trouver un bon browser

== Javas'Cool Builder ==

  * (`*`3) Créer une doc pour l'usage des proglets sous netbeans

  * (`*`3) Voir comment gérer le fait que les noms de fichiers de docs ne peuvent avoir d'accent bien que utilises comme titre des pages de docs ouvertes en "newtab"

== Javas'Cool Proglets ==

Voir aussi les *proglets en [http://wiki.inria.fr/sciencinfolycee/JavaScool:FaqD%C3%A9veloppement#Quelles_sont_les_proglets_en_cours_de_d.C3.A9veloppement_.3F développement]* et *en [http://wiki.inria.fr/sciencinfolycee/JavaScool:FaqD%C3%A9veloppement#Quelles_sont_les_proglets_en_projet_.3F projet]*.

  * (`*`2) Proposer un plugin netbeans

== Javas'Cool Web ==

  * (`*`3) Ajouter une page outil qui traduise du `[X]HTML` en HML à partir de work/src/org/javascool/builder/htm2hml.xslt

  * (`*`4) Proposer un mécanisme XML/XSLT pour produire les pages du site et gérer les liens, voir la production des pages syndiquées.

  * (`*`4) Détecter l'OS ds ?page=run pour highliter l'OS du user.

== Javas'Cool Platform ==

 * (`*`2) Voir à passer en javadoc 1.7 pour y gagner en présentation

 * (`*`2) Rendre disponible `TextFileEditor` & `TextFilesEditor` (avec boutons et interface) et `DoubleTabbedPane` sortis de org/javascool/gui

 * (`*`2) Améliorer l'usage de ant
   * offrir le jar de ant 1.8 en lib/ant 
   * mieux contrôler ce qui s'écrit en sortie : rendre silencieux les messages inutiles

 * (`*`3) Finaliser l'usage de `AntMenu` (libs ant sans exec + éditeur + ..) avec tooltips/description.

 * (`*`3) Déployer dans svn:work/lib la construction de autocomplete.jar

 * (`*`4) Voir à se libérer de https://gforge.inria.fr/projects/javascool pour le site web